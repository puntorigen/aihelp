// src/actions.js
module.exports = {
    executeAction: function(folderPath, prompt) {
      console.log(`Action executed on folder: ${folderPath} with prompt: ${prompt}`);
      // Your logic here
    }
  };
  